<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rustto.manager.mapper.SysMenuMapper">

    <resultMap id="sysMenuMap" type="com.rustto.manager.entity.SysMenu">
        <id property="id" column="id"/>
        <result property="parentId" column="parent_id"/>
        <result property="menuName" column="menu_name"/>
        <result property="menuType" column="menu_type"/>
        <result property="perms" column="perms"/>
        <result property="path" column="path"/>
        <result property="component" column="component"/>
        <result property="icon" column="icon"/>
        <result property="sort" column="sort"/>
        <result property="visible" column="visible"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <!-- 非超管：取用户可访问的菜单（经角色关联 + 启用过滤），按 sort 升序。
         含 visible=0 的隐藏路由（如工作流设计器），前端注册路由用，侧边栏按 visible 自行隐藏。 -->
    <select id="selectMenusByUserId" resultMap="sysMenuMap">
        SELECT DISTINCT m.*
        FROM sys_role_menu rm
        JOIN sys_menu m       ON m.id = rm.menu_id
        JOIN sys_user_role ur ON ur.role_id = rm.role_id
        JOIN sys_role r       ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND r.status = 1
          AND m.status = 1
        ORDER BY m.sort ASC
    </select>

</mapper>
